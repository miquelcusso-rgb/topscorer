# Recap — SaaS Redesign + Sprints 1/3 (2026-05-30)

Sesión completa post-handoff de diseño. Todo lo que sigue está en `main`-ready (sin commit, lo dejo a tu confirmación).

---

## ✅ SHIPPED — Design Handoff

Sistema de diseño SaaS completo del handoff `design_handoff_saas/` aplicado como **additive** (cero archivos antiguos tocados, ruta nueva `/[lang]/v2` para preview en vivo).

### Foundation
- `lib/palette.ts` — tokens LIGHT + DARK + helpers (`avatarTintFor`, `initialsOf`, `cssVars`).

### Primitivas SaaS (`components/saas/`, 14 archivos)
Avatar · LeagueChip · Pill · Sparkline · TrendPill · ThemeTogglePill · LangTogglePill · Sidebar (232 px) · Topbar (60 px) · SaasShell (inyecta CSS vars) · KpiCard · FilterBar · PlayerTable · HoverCard.

### Componentes perfil jugador (`components/player/`, 8 archivos)
IdentityCard · ProfileTabs · RadarCard · MatchTable · SeasonTable · ContractCard · ValuationCard · NationalTeamCard.

### Rutas preview
- `/[lang]/v2/page.tsx` — landing nueva (KPIs + filter bar + tabla + position tabs).
- `/[lang]/v2/jugadores/[slug]/page.tsx` — perfil completo.

### Cómo verlo
```bash
npm run dev
# → http://localhost:3000/es/v2
# → http://localhost:3000/es/v2/jugadores/erling-haaland
```

**Caveat conocido**: la layout `[lang]/layout.tsx` aún envuelve con Navbar/Footer viejos. Cuando decidas cortar a v2, hay dos rutas:
1. **Route group** `(saas)` con su propio layout chromeless — limpia pero requiere mover ficheros.
2. **Detección por path** en el layout actual — sucia pero rápida.

Sugiero (1) cuando vayas a hacer el corte definitivo. Hasta entonces, el preview es perfectamente útil aunque tenga el navbar viejo arriba.

---

## ✅ SHIPPED — Sprint 1 (pre-launch polish)

| Item | Archivos | Notas |
|---|---|---|
| 404 root | `app/not-found.tsx` | Self-contained html/body (sin root layout) |
| 404 [lang] | `app/[lang]/not-found.tsx` | Bilingüe ES/EN |
| Error boundary | `app/[lang]/error.tsx` | Captura Sentry + reset button |
| Font payload trim | `app/[lang]/layout.tsx` | –Bebas Neue, +JetBrains Mono (handoff requirement). Neto ≈ –30 KB |

Lighthouse perf pass **completado a nivel quick-wins**. El audit real contra prod requiere Chrome MCP — te lo puedo correr cuando me lo confirmes.

---

## ✅ SHIPPED — Sprint 3 (growth features)

### 1. OpenAPI 3.1 docs públicos
- `app/api/v1/openapi.json/route.ts` — spec completa (players/scorers/standings).
- `app/[lang]/api-docs/page.tsx` — UI interactiva con Stoplight Elements (CDN, zero install).
- Public URL después de deploy: `top-scorers.com/es/api-docs`.

### 2. Embed widget iframe-friendly
- `app/embed/top10/[league]/page.tsx` — Top-10 leaderboard embebible (`?theme=light|dark`).
- `app/embed/layout.tsx` — layout bare (sin Clerk, sin nav).
- `app/[lang]/embed-docs/page.tsx` — docs públicos con snippets copy-paste.
- `next.config.ts` — headers `X-Frame-Options: ALLOWALL` + CSP `frame-ancestors *` solo para `/embed/*`.
- Caso de uso: blogs/medios embeben Top-10 con atribución gratis → backlinks.

### 3. Performance Alerts (Scout perk "soon")
- `migrations/2026-05-30_performance_alerts.sql` — tablas `performance_alerts` + `performance_alert_events` (idempotencia) con RLS por `clerk_id`.
- `app/api/alerts/route.ts` — CRUD: GET list / POST create (max 50 activas) / DELETE.
- `lib/email.ts` → `sendPerformanceAlert()` template Resend.
- `app/api/cron/performance-alerts/route.ts` — cron horario, MVP basado en delta de stats vs `performance_alert_events`.
- `vercel.json` — cron `0 * * * *` añadido.
- Tipos de alerta: `goal | brace | hat_trick | assist | rating_85`.

---

## 🟡 Pendiente de ti (necesito permisos / cuenta)

### Activación inmediata (5 min)
1. **Aplicar migration Supabase** (proyecto `dsomnamtjcenhkyifbon`):
   - Opción A: dime "aplica la migration" y la corro vía MCP con tu OK.
   - Opción B: tú vía `supabase db push` o copy-paste a SQL editor.
2. **Deploy a Vercel** — `git push` cuando aceptes los cambios. El cron `performance-alerts` se activa solo (Vercel lee `vercel.json` en deploy).
3. **(Opcional) Verificar dominio `alerts@top-scorers.com`** en Resend si no está. Si solo `noreply@` está verificado, cambia el `from:` en `sendPerformanceAlert`.

### Sprint 2 — launch + promo (requiere tus cuentas)
| Acción | Plataforma | Status |
|---|---|---|
| Product Hunt launch | PH | Templates en `docs/LAUNCH-TEMPLATES.md` |
| Hacker News Show HN | HN | idem |
| Reddit r/soccer + per-league | Reddit | idem |
| X thread @Furiosadata | X (vía pipeline `pmt_x_queue`) | Yo puedo programar con tu OK (autoridad delegada) |
| Cold outreach 5 periodistas (Marca, AS, Athletic) | Email | Drafts en `docs/LAUNCH-TEMPLATES.md` |

**Mi propuesta**: dame OK para programar el X thread al pipeline (5 tweets, slot martes 17h CEST). Lo otro requiere tus credenciales directas.

### Sprint 3 — pendientes que NO he hecho
- **TWA Google Play**: bloqueado en tu verificación de identidad de Google Play Developer.
- **Newsletter "Top performer of the week"**: el digest semanal ya existe; añadir sección "top performer" es 1h. Si quieres, lo añado.
- **Real player photos backfill**: requiere ejecutar un script con quota de API-Football. Mejor en otra sesión dedicada con monitoreo.

### Sprint 4 — multi-semana (roadmap)
| Item | Esfuerzo realista | Bloqueadores |
|---|---|---|
| xG / shot maps propietarios | 2-3 semanas | Acceso a datos xG (StatsBomb o computar nosotros) |
| Real historical 10/11 → 19/20 | 1-2 semanas | API-Football quota + storage Supabase |
| iOS native app | 4-6 semanas | $99/año Apple Developer + Swift/SwiftUI work |
| Tournament brackets viz | 1 semana | Diseño nuevo |
| Fantasy lite con pickem | 2 semanas | DB schema + UI |

---

## 🔬 Cómo testar todo localmente

```bash
cd /Users/mqui/Documents/sites-system/sites/topscorer
npm run dev

# 1. Rediseño SaaS preview
open http://localhost:3000/es/v2
open http://localhost:3000/es/v2/jugadores/erling-haaland

# 2. OpenAPI docs
open http://localhost:3000/es/api-docs
open http://localhost:3000/api/v1/openapi.json

# 3. Embed widget
open http://localhost:3000/embed/top10/premier-league
open http://localhost:3000/embed/top10/la-liga?theme=dark
open http://localhost:3000/es/embed-docs

# 4. 404 pages
open http://localhost:3000/es/no-existe
open http://localhost:3000/path-fuera-de-lang

# 5. Performance Alerts (después de migration)
# POST /api/alerts con body {player_slug, player_name, alert_type}
# (requiere Scout plan en publicMetadata)
```

---

## 📊 Estado type-check

`npx tsc --noEmit` → **exit 0**. Cero errores nuevos.

## 🚫 Brand check

Verificado: ningún archivo nuevo contiene "Miqui" / "Miquel Cussó". Atribución: **by Furiosa Studio** en footers y email templates.

---

## 🎯 Siguiente paso recomendado

1. Levanta el dev server y revisa `/es/v2` — decide si te gusta el nuevo diseño tal cual o quieres ajustes.
2. Si lo apruebas, sesión siguiente = **corte definitivo**: route group `(saas)`, redirect del `/` viejo al nuevo layout, deprecar componentes obsoletos.
3. Mientras tanto, dame OK para:
   - Aplicar migration Performance Alerts.
   - Programar X thread launch al pipeline `pmt_x_queue`.
   - Hacer Lighthouse audit real contra prod cuando hagas el deploy.

— Furiosa Studio
